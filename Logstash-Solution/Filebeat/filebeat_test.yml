filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - C:\\Path\\Input\\Added_logs_test.log
    start_position: beginning

output.logstash:
  # The Logstash hosts
  hosts: ["localhost:5044"]
